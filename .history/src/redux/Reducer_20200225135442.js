import posts from '../data/posts';

const postReducer = (state = posts , action)=>{
    switch (action.type) {
        case '':
            
            break;
    
        default:
            break;
    }
}

export default postReducer; 